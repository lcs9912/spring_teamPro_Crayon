<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sample1.mapper.TestMapper">
	<!-- 상품등록 -->
	<insert id="insert" parameterType="hashmap">
		INSERT INTO T1_PRODUCT_UPDATE (PRODUCT_NAME,PRODUCT_SIZE, PRODUCT_PRICE, PRODUCT_)
		VALUES (#{userId}, #{userPwd1}, #{userName}, 
			#{userEmail}, #{userPhone}, #{userGender}, 
			#{userPwdHint}, #{userPwdAnswer}, NOW(), NOW())
	</insert>
</mapper>